{
  "name": "mychurchcalling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "convex:dev": "convex dev --run-path backend/convex",
    "convex:deploy": "convex deploy --run-path backend/convex"
  },
  "dependencies": {
    "@workos-inc/node": "^7.36.0",
    "convex": "^1.21.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
